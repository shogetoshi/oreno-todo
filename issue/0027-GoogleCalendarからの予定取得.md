- GoogleCalendarから予定を取得します。
    - 今はサンプル値を返すだけのダミー実装になっている部分を実際に実装します。
    - 下記の値を含むjsonファイルを仮で配置しますので、一旦それを使って実装してください。
        - "$HOME/Library/Application Support/oreno-todo/google-calendar.json"
        - この部分は後に修正するので、ファイルパスなども直書きで修正しやすいもので良いです。
            - ただしホームディレクトリのパスをあらわに書かないように気をつけて
    - これによって作成される、refresh_tokenを含む情報はmacOSのキーチェーンに保存するようにしてください。
        - 可能なら他OSでも動くコードにしたいですが、まずはmacOSで動けばOKです。
